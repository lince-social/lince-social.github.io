This uses Maud for templating HTML with Rust. use p.class1.class2 { "Hello" } instead of p class="class1 class2" { "Hello" }. If it's a div with classes no need to include div, just .class1.class2 { "Hello" }. Whenever you see code that is not implemented like this, enforce this pattern of simplification.

Dont use playwright.

Lince has a feminine pronoun in portuguese (a Lince, not o Lince).

When changing the Rust part, make sure to check the src/i18n.rs file and read the english version, translate the text into the other languages if they dont match.

If the user provides a TMIL Typst file path and says "translate", translate only item title and subtitle to pt-BR and zh-CN side by side (tmil_tr), and keep body text unchanged.

Do not put commends, do not speak or ask me what to implement next.
